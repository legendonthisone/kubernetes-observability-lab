apiVersion: apps/v1
kind: Deployment
metadata:
  name: loadgen
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loadgen
  template:
    metadata:
      labels:
        app: loadgen
    spec:
      containers:
      - name: loadgen
        image: busybox:1.36
        command: ["/bin/sh","-c"]
        args:
        - while true; do wget -q -O /dev/null http://agent:5000/work 2>/dev/null; wget -q -O /dev/null http://agent:5000/health 2>/dev/null; sleep 1; done
        resources:
          requests:
            cpu: 10m
            memory: 16Mi
